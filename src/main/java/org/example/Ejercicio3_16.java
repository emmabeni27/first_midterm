package org.example;
import java.util.Date;
import java.util.Queue;
/*public static int[] readInts(String name)
    {
        In in = new In(name);
        Queue<Date> q = new Queue<Integer>();
        while (!in.isEmpty())
            q.enqueue(in.readInt());
        int N = q.size();
        int[] a = new int[N];
        for (int i = 0; i < N; i++)
            a[i] = q.dequeue();
        return a;
    }*/

/*public class Ejercicio3_16{
    static Date[] readDates(String date){
        In in = new In(date); //inicializo esrtuctura In
        Queue<Date> myQueue = new Queue<>();
        while(!in.isEmpty()){
            myQueue.enqueue(new Date(in.readString())); // leo las fechas de un archivo y las guardo en un array

        }
    int n =myQueue.size();
    Date[] dates =new Date[n];
    for (int i=0; i<n; i++){
        dates[i]=queue.dequeue();
        return Dates;
    }
    }
}*/
